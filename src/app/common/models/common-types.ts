export interface CommonList {
    value: number;
    option: string;
}

export interface GenderList {
    value: string;
    option: string;   
}

export interface TableErrorMessage {
    message: string,
    desc: string
}